import * as actionTypes from './actionTypes'
import * as controlOptions from './controlOptions'
import * as presentation from './presentation'

export default {...actionTypes, ...controlOptions, ...presentation}